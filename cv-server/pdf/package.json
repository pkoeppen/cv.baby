{
  "name": "cvbaby-pdf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy:dev": "npm run package && serverless deploy --stage dev && serverless s3deploy --stage dev",
    "deploy:prod": "npm run package && serverless deploy --stage prod && serverless s3deploy --stage prod",
    "package": "rm -f package.zip && make package.zip"
  },
  "devDependencies": {
    "serverless-plugin-existing-s3": "^2.4.0"
  }
}
