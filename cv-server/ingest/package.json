{
  "name": "cvbaby-ingest",
  "private": true,
  "scripts": {
    "deploy:dev": "serverless deploy --stage dev && serverless s3deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod && serverless s3deploy --stage prod",
    "install-sharp": "rm -rf ./node_modules && npm install --arch=x64 --platform=linux --target=8.10.0 sharp",
    "package": "rm -f ingest.zip && zip a -r ingest.zip ./*"
  },
  "dependencies": {
    "serverless-plugin-existing-s3": "^2.4.0",
    "sharp": "^0.22.1"
  }
}
